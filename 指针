#include<stdio.h>
#include<string.h>
#define chang 100
int main()
{
	char a[chang];
	get(a);
	int in=0;
    for(int i=0;i<strlen(a);i++)
	{
		if((a[i]=='i'||'I')&&a[i+1]=='n')
		{
			in++;
		}
	}
	printf("%d",in);
	return 0;
}
